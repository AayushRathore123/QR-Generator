import { DisableCutCopyPasteDirective } from './disable-cut-copy-paste.directive';

describe('DisableCutCopyPasteDirective', () => {
  it('should create an instance', () => {
    const directive = new DisableCutCopyPasteDirective();
    expect(directive).toBeTruthy();
  });
});
